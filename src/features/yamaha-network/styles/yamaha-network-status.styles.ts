import styled from "styled-components";

export const NetworkStatus = styled.main`
  align-items: center;
  background: #0a0a0a;
  color: #ffffff;
  display: flex;
  font-size: 14px;
  inset: 0;
  justify-content: center;
  letter-spacing: 0.02em;
  position: fixed;
`;
